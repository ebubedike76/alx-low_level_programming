#include <stdio.h>
/**
 * main - A program that prints all single digit numbers from zero to ten.
 *
 * Return: Always 0 (Success)
 *
 */
int main(void)
{
	int x;

	for (x = 0; x < 10; x++)
		printf("%d", x);
	printf("\n");
	return (0);
}
